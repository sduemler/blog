---
import Post from "../components/Post.astro";

const { allPosts } = Astro.props;

// allPosts arrives already filtered and sorted by getPosts().
const posts = allPosts.slice(0, 5);
---

<content>
	<ul class="posts-list">
		{posts.map((post) => <Post post={post} />)}
	</ul>
</content>
